server:
  port: 8082

spring:
  application:
    name: ORDER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/order-db
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
