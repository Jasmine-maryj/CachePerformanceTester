server:
  port: 8081

spring:
  application:
    name: PRODUCT-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/product-db
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
